{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "independent",
  "npmClient": "yarn",
  "packages": [
    "packages/*"
  ],
  "command": {
    "version": {
      "message": "chore: publish",
      "allowBranch": "master",
      "conventionalCommits": true
    },
    "publish": {
      "conventionalCommits": true,
      "allowBranch": "master"
    }
  }
}
